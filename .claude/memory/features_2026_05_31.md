---
name: features-2026-05-31
description: "2026-05-31/06-01 qo'shilgan featurelar — kassa standartlari, statsionar qarzdorlar, super analitika, jurnal yaxshilanishlari."
metadata: 
  node_type: memory
  type: project
  originSessionId: 10d0df07-9d29-4572-a47f-bf5f8e147e96
---

# 2026-05-31 / 06-01 featurelar

Bir kunda qo'shilgan asosiy ishlar (commitlar bilan):

## Kassa standartga yetkazildi (`1638064`)
`apps/web-clinic/src/pages/cashier.tsx` + `shift-bar.tsx`:
- **Chek qayta chop etish** — To'lovlar ro'yxatida har tx'da "Chek" tugmasi (`paymentReceiptHtml`+`printReceiptHybrid`, reception patterni).
- **X-hisobot** — smena ochiq bo'lganda "X-hisobot" (ShiftReportDialog title closed_at bo'yicha X/Z ni ajratadi).
- **Custom sana oralig'i + CSV export** — PresetFilter'ga "Oraliq", header'da Export. `transactions` endpoint `limit` query oladi.
- **Vozvrat/Void uchun PIN** — `api.shifts.verifyActivePin(pin)` majburiy (VoidTransactionDialog, RefundDialog).

## Statsionar qarzdorlar (`f9e32f5`, `d17f8b7`)
- Migration: `inpatient_stays.debt_reason`.
- `inpatient.module.ts` `debtors()` endpoint + `@Get('debtors')`: faol (admitted, balans<0) + chiqarilgan qarzdorlar (to'liq: tel/manzil/qarovchi/qarz sababi/kun/shifokor/xona).
- Discharge: qarz bilan chiqarishda (force) **debt_reason majburiy**.
- `inpatient.tsx` "Qarzdorlar" tab + InpatientDebtorsView. Chiqarilgan qarzdorga: **"Qarz yopish"** (DebtPayDialog → addLedger deposit + to'lov cheki avto) + **"Xizmatlar cheki"** (getStay + inpatientDischargeReceiptHtml).

## Super analitika (`3d376a7`, `885df59`)
- `analytics.module.ts`: custom sana oralig'i (rangeFor allaqachon qo'llaydi), `inpatientShare` davrga bog'landi (patient_ledger charge), `allDoctors`+`serviceDetail` endpointlar.
- Yangi sahifalar: `analytics-doctors.tsx` (barcha shifokor: tushum+komissiya), `analytics-services.tsx` (xizmat drill-down: kunlik+shifokorlar). `components/analytics/preset-bar.tsx` (umumiy DRY). Analitika bloklariga "Batafsil →".
- ESLATMA: "top xizmatga shifokor ustuni + jami qatori" (`58e8c34`) qo'shilgan, lekin user yoqtirmagani uchun REVERT qilingan (`723f3c1`).

## Jurnal yaxshilanishlari
- Moliya oynasi qotgan layout (KPI+filtr tepada, hisob pastda, faqat jadval scroll).
- Bekor qilingan amallar doim ko'rinadi (line-through, include_void=true) — `showVoid` checkbox olib tashlandi.
- Kassir ustuni = smenadagi navbatchi operator (shift_id→shift_operators), login user emas.
- Qabulxonachiga jurnal menyusi + tx edit/delete/void uchun receptionist roli (`487735d`).

## Kichik fixlar
- Qabulxona savatchada tanlangan shifokor ko'rinadi; "Qabulni yakunlash" tasdiq oynasi (chek bilan/cheksiz/ortga); ommabop xizmat sonidan × belgisi olib tashlandi (`f34f609`).
- Smena hisobotida kassir=navbatchi.

Bog'liq: [[clary-app-skill]], [[inpatient-feature]], [[data-admin-feature]], [[payroll-smart]]
